# 08_12_自動縮尺設計

## 1. 目的
Flow全体を指定用紙へ収め、レビュー時に全体像を把握できるPDFを生成する。

## 2. 縮尺方式
Canvas外接矩形とPDF描画可能領域から縮尺を算出する。縦横比は維持し、歪ませない。

## 3. 最小縮尺
最小縮尺を下回る場合は自動的にページ分割を推奨する。読めないPDFを正常出力として扱わない。

## 4. 余白
余白はExportOptionsで指定する。初期値は上下左右15mmとする。

## 5. 完了条件
小規模Flowは1ページに収まり、大規模Flowは縮尺限界を判定できる。
